- hosts:  tag_Name_core_bastion_host tag_Name_ecs_bastion_host common-nginx-proxy-1  common-httpd-proxy-1 key_core_instances:!tag_Name_core_nginx_proxy_1:!tag_Name_core_freeipa_master_1:!tag_Name_core_freeipa_replica_1
  vars:
    datadog_api_key: "{{credstash_query_output.stdout}}"
    datadog_config:
      hostname: "{{ ec2_tag_Account}}-{{ ec2_tag_Name }}-{{ inventory_hostname}}"
      expvar_port: 5050
      cmd_port: 5051
  user: ubuntu
  become: yes
  become_method: sudo

  roles:
    - role: packages-ubuntu
    - role: users
    - role: datadog
    - role: filebeat-ubuntu
    - role: hosts



- hosts: common-nginx-proxy-1 common-httpd-proxy-1
  user: ubuntu
  become: yes
  become_method: sudo
  roles:
    - role: filebeat-nginx

- hosts: key_ecs_instances

  vars:
    datadog_api_key: "{{credstash_query_output.stdout}}"
    datadog_config:
      hostname: "{{ ec2_tag_Account}}-{{ ec2_tag_Name }}-{{ inventory_hostname}}"
      expvar_port: 5050
      cmd_port: 5051
  user: ubuntu
  become: yes
  become_method: sudo

  roles:
    - role: packages-ubuntu
    - role: users
    - role: datadog
    - role: datadog-docker



- hosts: tag_Name_core_freeipa_master_1 tag_Name_core_freeipa_replica_1
  vars:
    datadog_api_key: "{{credstash_query_output.stdout}}"
    datadog_config:
      hostname: "{{ ec2_tag_Account}}-{{ ec2_tag_Name }}-{{ inventory_hostname}}"
  user: fedora
  become: yes
  become_method: sudo

  roles:
     - role: packages-centos
     - role: users
     - role: datadog
     - role: filebeat-centos

- hosts: tag_Name_core_gocd_agent_ansible_1
  user: ubuntu
  become: yes
  become_method: sudo
  roles:
    - role: packer

- hosts:  common-nginx-proxy-1 key_ecs_instances   key_core_instances:!tag_Name_core_nginx_proxy_1:!tag_Name_core_freeipa_master_1:!tag_Name_core_freeipa_replica_1:!tag_Name_core_gocd_agent_ansible_1:!tag_role_core_gocd_agent
  vars:
    freeipa_system_user: "{{credstash_freeipa_output.stdout}}"
  user: ubuntu
  become: yes
  become_method: sudo

  roles:
   - role: resolv_conf
   - role: freeipa-client

   - hosts: tag_Name_core_gocd_agent_ansible_1
     vars:
       freeipa_system_user: "{{credstash_freeipa_output.stdout}}"

     user: ubuntu
     become: yes
     become_method: sudo

     roles:
       - role: packer
       - role: freeipa-client

   - hosts: tag_role_core_gocd_agent
     vars:
       freeipa_system_user: "{{credstash_freeipa_output.stdout}}"

     user: ubuntu
     become: yes
     become_method: sudo

     roles:
       - role: freeipa-client
