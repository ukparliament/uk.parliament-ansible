- copy:
    src: files/filebeat-bitnami-wordpress.yml
    dest: /etc/filebeat/filebeat.yml
    owner: root
    group: root
    mode: 0600

- copy:
    src: files/apache2-module.yml
    dest: /etc/filebeat/modules.d/apache2.yml


- service:
    name: filebeat
    state: restarted
